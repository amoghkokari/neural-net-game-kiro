# Build requirements for creating executables
# Install with: pip install -r requirements-build.txt

# Core game dependencies (from requirements.txt)
pygame==2.5.2
numpy==1.24.3
matplotlib==3.7.2
pygame-gui==0.6.9
pyttsx3==2.90

# Testing dependencies
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0

# Build tools
pyinstaller==6.3.0
setuptools>=65.0.0

# Optional: For better compression
upx-ucl; platform_system != "Darwin"  # UPX not recommended for macOS